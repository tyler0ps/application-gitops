apiVersion: v1
kind: Service
metadata:
  name: demo-redis
  namespace: demo-apps
  labels:
    app: demo-redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: redis
    protocol: TCP
    name: redis
  selector:
    app: demo-redis
